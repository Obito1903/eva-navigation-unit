import { Theme } from "../theme.slint";
import { Dropdown } from "../components/dropdown.slint";
import { CapsuleBarSlider } from "../components/capsule_bar_slider.slint";
import { Toggle } from "../components/toggle.slint";

// UI category settings tab.
export component SettingsUiTab inherits Rectangle {
    background: transparent;

    // Color theme: 0 = NERV-HQ | 1 = MATRIX
    in-out property <int> theme-id: 0;
    callback theme-changed(int);

    // Background wireframe model: 0 = sphere | 1 = cube | 2 = car
    in-out property <int> gfx-model: 0;
    callback gfx-model-changed(int);

    // Enables the background's frosted-glass blur/tint post pass.
    in-out property <bool> gfx-frost-enabled: true;
    callback gfx-frost-enabled-changed(bool);

    // Background render brightness multiplier (0.0 - 1.5).
    in-out property <float> gfx-bg-brightness: 1.0;
    callback gfx-bg-brightness-changed(float);
    property <float> min-bg-brightness: 0.0;
    property <float> max-bg-brightness: 1.5;

    // View transition mode: 0 = CRT | 1 = FADE | 2 = SLIDE
    in-out property <int> transition-mode: 0;
    callback transition-changed(int);

    // Android Auto video start/stop transition: 0 = CRT | 1 = FADE | 2 = SLIDE
    in-out property <int> aa-video-transition-mode: 1;
    callback aa-video-transition-changed(int);

    // Transition speed multipliers (1.0 = default; higher = faster).
    in-out property <float> transition-speed: 1.0;
    callback transition-speed-changed(float);
    in-out property <float> aa-video-transition-speed: 1.0;
    callback aa-video-transition-speed-changed(float);

    // Selectable speed range (multiplier).
    property <float> min-speed: 0.25;
    property <float> max-speed: 3.0;

    // Fullscreen window mode.
    in-out property <bool> fullscreen: false;
    callback fullscreen-changed(bool);

    VerticalLayout {
        alignment: start;
        padding: 24px;
        spacing: 20px;

        Text {
            text: "UI";
            color: Theme.text;
            font-size: 20px;
            font-weight: 800;
            letter-spacing: 3px;
        }

        Rectangle {
            height: 1px;
            background: Theme.red-dim;
        }

        // ── Appearance ────────────────────────────────────────────────
        VerticalLayout {
            spacing: 14px;

            Text {
                text: "APPEARANCE";
                color: Theme.text-dim;
                font-size: 12px;
                font-weight: 700;
                letter-spacing: 2px;
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "COLOR THEME";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Dropdown {
                    width: 200px;
                    options: ["NERV-HQ", "MATRIX", "NEON"];
                    current-index <=> root.theme-id;
                    selected(i) => {
                        root.theme-changed(i);
                    }
                }
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "BACKGROUND MODEL";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Dropdown {
                    width: 200px;
                    options: ["SPHERE", "CUBE", "CAR", "SPEAKER"];
                    current-index <=> root.gfx-model;
                    selected(i) => {
                        root.gfx-model-changed(i);
                    }
                }
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "FROST EFFECT";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Toggle {
                    checked <=> root.gfx-frost-enabled;
                    toggled(on) => {
                        root.gfx-frost-enabled-changed(on);
                    }
                }
            }

            // Background render brightness
            VerticalLayout {
                spacing: 8px;

                HorizontalLayout {
                    spacing: 12px;

                    Text {
                        text: "BACKGROUND BRIGHTNESS";
                        color: Theme.text-dim;
                        font-size: 12px;
                        font-weight: 600;
                        letter-spacing: 1px;
                        vertical-alignment: center;
                    }

                    Text {
                        text: Math.round(root.gfx-bg-brightness * 100) + "%";
                        color: Theme.text;
                        font-size: 13px;
                        font-weight: 700;
                        vertical-alignment: center;
                        horizontal-stretch: 1;
                        horizontal-alignment: right;
                    }
                }

                CapsuleBarSlider {
                    height: 32px;
                    value: (root.gfx-bg-brightness - root.min-bg-brightness) / (root.max-bg-brightness - root.min-bg-brightness);
                    value-changed(v) => {
                        root.gfx-bg-brightness = clamp(root.min-bg-brightness + v * (root.max-bg-brightness - root.min-bg-brightness), root.min-bg-brightness, root.max-bg-brightness);
                        root.gfx-bg-brightness-changed(root.gfx-bg-brightness);
                    }
                }
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "FULLSCREEN";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Toggle {
                    checked <=> root.fullscreen;
                    toggled(on) => {
                        root.fullscreen-changed(on);
                    }
                }
            }
        }

        // ── Transitions ───────────────────────────────────────────────
        VerticalLayout {
            spacing: 14px;

            Text {
                text: "TRANSITIONS";
                color: Theme.text-dim;
                font-size: 12px;
                font-weight: 700;
                letter-spacing: 2px;
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "VIEW TRANSITION";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Dropdown {
                    width: 200px;
                    options: ["CRT", "FADE", "SLIDE"];
                    current-index <=> root.transition-mode;
                    selected(i) => {
                        root.transition-changed(i);
                    }
                }
            }

            // View transition speed
            VerticalLayout {
                spacing: 8px;

                HorizontalLayout {
                    spacing: 12px;

                    Text {
                        text: "SPEED";
                        color: Theme.text-dim;
                        font-size: 12px;
                        font-weight: 600;
                        letter-spacing: 1px;
                        vertical-alignment: center;
                    }

                    Text {
                        text: Math.round(root.transition-speed * 100) / 100 + "×";
                        color: Theme.text;
                        font-size: 13px;
                        font-weight: 700;
                        vertical-alignment: center;
                        horizontal-stretch: 1;
                        horizontal-alignment: right;
                    }
                }

                CapsuleBarSlider {
                    height: 32px;
                    value: (root.transition-speed - root.min-speed) / (root.max-speed - root.min-speed);
                    value-changed(v) => {
                        root.transition-speed = clamp(root.min-speed + v * (root.max-speed - root.min-speed), root.min-speed, root.max-speed);
                        root.transition-speed-changed(root.transition-speed);
                    }
                }
            }

            HorizontalLayout {
                spacing: 12px;

                Text {
                    text: "AA VIDEO TRANSITION";
                    color: Theme.text;
                    font-size: 14px;
                    font-weight: 600;
                    vertical-alignment: center;
                    horizontal-stretch: 1;
                }

                Dropdown {
                    width: 200px;
                    options: ["CRT", "FADE", "SLIDE"];
                    current-index <=> root.aa-video-transition-mode;
                    selected(i) => {
                        root.aa-video-transition-changed(i);
                    }
                }
            }

            // AA video transition speed
            VerticalLayout {
                spacing: 8px;

                HorizontalLayout {
                    spacing: 12px;

                    Text {
                        text: "SPEED";
                        color: Theme.text-dim;
                        font-size: 12px;
                        font-weight: 600;
                        letter-spacing: 1px;
                        vertical-alignment: center;
                    }

                    Text {
                        text: Math.round(root.aa-video-transition-speed * 100) / 100 + "×";
                        color: Theme.text;
                        font-size: 13px;
                        font-weight: 700;
                        vertical-alignment: center;
                        horizontal-stretch: 1;
                        horizontal-alignment: right;
                    }
                }

                CapsuleBarSlider {
                    height: 32px;
                    value: (root.aa-video-transition-speed - root.min-speed) / (root.max-speed - root.min-speed);
                    value-changed(v) => {
                        root.aa-video-transition-speed = clamp(root.min-speed + v * (root.max-speed - root.min-speed), root.min-speed, root.max-speed);
                        root.aa-video-transition-speed-changed(root.aa-video-transition-speed);
                    }
                }
            }
        }
    }
}
